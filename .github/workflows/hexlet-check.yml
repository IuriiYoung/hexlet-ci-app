name: hexlet-ci-app
on: push
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
            - run: npm setup
            - run: npm run lint
            - run: npm run tests